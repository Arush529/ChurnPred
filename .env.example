PORT=5000
# Replace with your local MongoDB or MongoDB Atlas URI:
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/churnpred?retryWrites=true&w=majority
JWT_SECRET=churnpred_super_secret_jwt_key_2026_prod

# Optional: Google Gemini API Key for Churn AI Chatbot (Falls back to zero-config diagnostic engine if omitted)
GEMINI_API_KEY=your_gemini_api_key_here
